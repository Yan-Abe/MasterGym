import { StyleSheet } from 'react-native'
import { theme } from '../../global/styles/theme'

//estilização da
export const styles = StyleSheet.create({
  container: {
    minHeight: 120,
    maxHeight: 120,
    paddingLeft: 24 //a lista pode ir até 24em para a direita
  }
})
